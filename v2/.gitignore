node_modules/
dist/
song-server.config.json

# Regenerated on every `npm run dev`/`npm run build` (bake-songs / compile-uikitml run first
# in both scripts) - committed copies only ever get overwritten, never read as-is.
public/songs/manifest.json
public/ui/*.json
